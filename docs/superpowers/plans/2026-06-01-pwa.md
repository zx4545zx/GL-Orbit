# PWA Basic (Installable) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เพิ่ม PWA support ระดับ Basic — ให้ GL-Orbit ติดตั้งเป็นแอปได้ (Add to Home Screen) ด้วย `@vite-pwa/sveltekit`

**Architecture:** ใช้ SvelteKitPWA plugin จัดการ manifest, service worker, meta tags ให้อัตโนมัติ ไม่มี offline caching — ข้อมูลทุกอย่างยังโหลดจาก server ปกติ

**Tech Stack:** `@vite-pwa/sveltekit`, SvelteKit 2.x, Vite 6.x, Tailwind CSS 4

---

## File Structure

| ไฟล์ | Action | หน้าที่ |
|------|--------|--------|
| `package.json` | Modify | เพิ่ม `@vite-pwa/sveltekit` ใน devDependencies |
| `vite.config.ts` | Modify | เพิ่ม SvelteKitPWA plugin config |
| `src/service-worker.ts` | Create | Service worker — precache shell assets |
| `src/app.html` | Modify | แทนที่ favicon SVG ด้วยไฟล์ .ico จริง |

---

### Task 1: Install PWA Dependencies

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Install `@vite-pwa/sveltekit`**

```bash
npm install -D @vite-pwa/sveltekit
```

Expected: ติดตั้งสำเร็จ เพิ่มใน `devDependencies`

- [ ] **Step 2: Verify installation**

```bash
ls node_modules/@vite-pwa/sveltekit
```

Expected: โฟลเดอร์มีอยู่

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "deps: add @vite-pwa/sveltekit for PWA support"
```

---

### Task 2: Generate PWA Icons from Source

**Files:**
- Create: `static/icons/pwa-192x192.png`
- Create: `static/icons/pwa-512x512.png`
- Create: `static/icons/apple-touch-icon-180x180.png`

- [ ] **Step 1: Generate 192x192 icon**

```bash
sips -z 192 192 static/icons/gl-orbit-icon.png --out static/icons/pwa-192x192.png
```

Expected: `static/icons/pwa-192x192.png` created

- [ ] **Step 2: Generate 512x512 icon**

```bash
sips -z 512 512 static/icons/gl-orbit-icon.png --out static/icons/pwa-512x512.png
```

Expected: `static/icons/pwa-512x512.png` created

- [ ] **Step 3: Generate Apple touch icon (180x180)**

```bash
sips -z 180 180 static/icons/gl-orbit-icon.png --out static/icons/apple-touch-icon-180x180.png
```

Expected: `static/icons/apple-touch-icon-180x180.png` created

- [ ] **Step 4: Commit icon files**

```bash
git add static/icons/pwa-192x192.png static/icons/pwa-512x512.png static/icons/apple-touch-icon-180x180.png
git commit -m "assets: add PWA icon sizes (192, 512, apple-touch)"
```

---

### Task 3: Configure PWA Plugin

**Files:**
- Modify: `vite.config.ts`

- [ ] **Step 1: Replace `vite.config.ts` content**

Replace the entire file with:

```typescript
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';
import tailwindcss from '@tailwindcss/vite';
import { SvelteKitPWA } from '@vite-pwa/sveltekit';

export default defineConfig({
	plugins: [
		tailwindcss(),
		sveltekit(),
		SvelteKitPWA({
			registerType: 'autoUpdate',
			manifest: {
				name: 'GL-Orbit',
				short_name: 'GL-Orbit',
				description: 'ศูนย์กลางข้อมูลและตารางฉายซีรีส์ GL',
				theme_color: '#FF6B9D',
				background_color: '#FFF5F7',
				display: 'standalone',
				lang: 'th',
				icons: [
					{
						src: '/icons/pwa-192x192.png',
						sizes: '192x192',
						type: 'image/png'
					},
					{
						src: '/icons/pwa-512x512.png',
						sizes: '512x512',
						type: 'image/png'
					},
					{
						src: '/icons/pwa-512x512.png',
						sizes: '512x512',
						type: 'image/png',
						purpose: 'maskable'
					},
					{
						src: '/icons/apple-touch-icon-180x180.png',
						sizes: '180x180',
						type: 'image/png'
					}
				]
			},
			workbox: {
				globPatterns: ['**/*.{html,css,js,woff2}']
			}
		})
	]
});
```

- [ ] **Step 2: Type check**

```bash
npm run check
```

Expected: no type errors related to vite config

- [ ] **Step 3: Commit**

```bash
git add vite.config.ts
git commit -m "feat: configure SvelteKitPWA plugin with manifest and icons"
```

---

### Task 4: Create Service Worker

**Files:**
- Create: `src/service-worker.ts`

- [ ] **Step 1: Create `src/service-worker.ts`**

Write file:

```typescript
/// <reference types="@sveltejs/kit" />
/// <reference no-default-lib="true"/>
/// <reference lib="esnext" />
/// <reference lib="webworker" />

import { build, files, version } from '$service-worker';

const sw = self as unknown as ServiceWorkerGlobalScope;

const CACHE = `cache-${version}`;

const ASSETS = [
	...build,
	...files
];

sw.addEventListener('install', (event) => {
	event.waitUntil(
		caches.open(CACHE).then((cache) => cache.addAll(ASSETS))
	);
});

sw.addEventListener('activate', (event) => {
	event.waitUntil(
		caches.keys().then((keys) => {
			return Promise.all(
				keys.filter((key) => key !== CACHE).map((key) => caches.delete(key))
			);
		})
	);
});

sw.addEventListener('fetch', (event) => {
	if (event.request.method !== 'GET') return;

	event.respondWith(
		caches.match(event.request).then((cached) => {
			return cached || fetch(event.request);
		})
	);
});
```

- [ ] **Step 2: Type check**

```bash
npm run check
```

Expected: no errors from `src/service-worker.ts`

- [ ] **Step 3: Commit**

```bash
git add src/service-worker.ts
git commit -m "feat: add service worker for PWA precaching"
```

---

### Task 5: Update Favicon

**Files:**
- Modify: `src/app.html`

- [ ] **Step 1: Replace favicon link in `src/app.html`**

Replace line 5:
```
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>💖</text></svg>" />
```

With:
```html
<link rel="icon" href="/icons/gl-orbit-icon.ico" />
```

- [ ] **Step 2: Commit**

```bash
git add src/app.html
git commit -m "feat: replace emoji favicon with GL-Orbit app icon"
```

---

### Task 6: Build and Verify

- [ ] **Step 1: Build the project**

```bash
npm run build
```

Expected: build สำเร็จ ไม่มี error

- [ ] **Step 2: Check generated files exist**

```bash
ls -la .svelte-kit/output/client/manifest.webmanifest
ls -la .svelte-kit/output/client/sw.js
```

Expected: ทั้งสองไฟล์มีอยู่

- [ ] **Step 3: Verify manifest content**

```bash
cat .svelte-kit/output/client/manifest.webmanifest | python3 -m json.tool
```

Expected: JSON มี `name: "GL-Orbit"`, `theme_color: "#FF6B9D"`, `icons` array

- [ ] **Step 4: Preview and run Lighthouse check**

```bash
npm run preview
```

เปิด Chrome DevTools > Lighthouse > ตรวจ PWA category — score ควร ≥ 90

- [ ] **Step 5: Commit if any build artifacts need tracking**

```bash
git status
git add ...
git commit -m "chore: finalize PWA build configuration"
```

---

### Task 7: Create Branch and PR

- [ ] **Step 1: Create feature branch**

```bash
git checkout -b feature/pwa-basic
```

- [ ] **Step 2: Push branch**

```bash
git push origin feature/pwa-basic
```

- [ ] **Step 3: Create PR to dev**

```bash
gh pr create --base dev --head feature/pwa-basic --title "feat: PWA basic support (installable)" --body "## Summary
เพิ่ม PWA support ระดับ Basic — ให้ GL-Orbit ติดตั้งเป็นแอปได้ (Add to Home Screen)

## Changes
- เพิ่ม @vite-pwa/sveltekit dependency
- คอนฟิก SvelteKitPWA plugin พร้อม manifest (ไทย), theme coral, icons
- เพิ่ม service worker สำหรับ precache shell assets
- แทนที่ emoji favicon ด้วย GL-Orbit app icon

## No offline caching
ข้อมูลทุกอย่าง (series, schedules, auth) ยังโหลดจาก server ปกติ"
```

Expected: PR URL แสดงขึ้นมา
