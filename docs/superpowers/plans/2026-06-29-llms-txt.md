# llms.txt Endpoint Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a crawlable `/llms.txt` endpoint that summarizes GL-Orbit and links LLM crawlers to key public pages.

**Architecture:** Implement a deterministic SvelteKit route endpoint at `src/routes/llms.txt/+server.ts`. The route will use `url.origin` to generate absolute URLs and return Markdown text with SEO-friendly cache headers, matching existing `robots.txt` and `sitemap.xml` endpoint conventions.

**Tech Stack:** SvelteKit 2, TypeScript, SvelteKit `RequestHandler` route endpoint.

## Global Constraints

- Use SvelteKit route endpoint pattern already present in `src/routes/robots.txt/+server.ts`.
- Use `import type { RequestHandler } from './$types.js';` because the project uses NodeNext module resolution.
- Return `content-type: text/markdown; charset=utf-8`.
- Return `cache-control: public, max-age=0, s-maxage=3600, stale-while-revalidate=86400`.
- Generate absolute links using `url.origin`.
- Include public links for `/`, `/series`, `/calendar`, `/artists`, and `/sitemap.xml`.
- Do not promote `/admin/`, `/api/`, `/login`, `/register`, `/profile`, or `/notifications`.
- No database, schema, auth, sitemap, or robots changes.

---

## File Structure

- Create `src/routes/llms.txt/+server.ts`: SvelteKit text endpoint for `/llms.txt`.
- No existing files are modified.

---

### Task 1: Add `/llms.txt` Route Endpoint

**Files:**
- Create: `src/routes/llms.txt/+server.ts`

**Interfaces:**
- Consumes: SvelteKit `RequestHandler` and request `url.origin`.
- Produces: GET endpoint at `/llms.txt` returning Markdown text and cache headers.

- [ ] **Step 1: Write the endpoint file**

Create `src/routes/llms.txt/+server.ts` with this exact content:

```ts
import type { RequestHandler } from './$types.js';

function absoluteUrl(origin: string, path: string): string {
	return new URL(path, origin).toString();
}

export const GET: RequestHandler = async ({ url }) => {
	const origin = url.origin;
	const body = [
		'# GL-Orbit',
		'',
		'GL-Orbit is a community-focused web app for Girls\' Love (GL) series fans. It helps visitors discover series, artists, and broadcast schedules with timezone-aware release information.',
		'',
		'## Key public pages',
		'',
		`- Home: ${absoluteUrl(origin, '/')}`,
		`- Series directory: ${absoluteUrl(origin, '/series')}`,
		`- Broadcast calendar: ${absoluteUrl(origin, '/calendar')}`,
		`- Artists directory: ${absoluteUrl(origin, '/artists')}`,
		`- XML sitemap: ${absoluteUrl(origin, '/sitemap.xml')}`,
		'',
		'## Crawler guidance',
		'',
		'Focus on public discovery pages and individual public detail pages linked from the sitemap. Admin, API, authentication, profile, and notification routes are not primary crawl targets.',
		''
	].join('\n');

	return new Response(body, {
		headers: {
			'content-type': 'text/markdown; charset=utf-8',
			'cache-control': 'public, max-age=0, s-maxage=3600, stale-while-revalidate=86400'
		}
	});
};
```

- [ ] **Step 2: Run TypeScript/Svelte validation**

Run:

```bash
npm run check
```

Expected: command exits successfully with no TypeScript or Svelte errors.

- [ ] **Step 3: Verify route content locally if a dev server is available**

Run in one terminal:

```bash
npm run dev
```

Then run in another terminal:

```bash
curl -i http://localhost:5173/llms.txt
```

Expected response includes:

```text
HTTP/1.1 200 OK
content-type: text/markdown; charset=utf-8
```

Expected body includes:

```text
# GL-Orbit
## Key public pages
## Crawler guidance
```

- [ ] **Step 4: Commit implementation**

Run:

```bash
git add src/routes/llms.txt/+server.ts
git commit -m "feat: add llms.txt endpoint"
```

Expected: commit succeeds and includes only the new endpoint file.

---

## Self-Review

- Spec coverage: The single task adds the requested `/llms.txt` endpoint, uses route endpoint architecture, absolute URLs, intended public links, guidance exclusions, and required headers.
- Placeholder scan: No TBD/TODO/placeholder text remains.
- Type consistency: Uses the same `RequestHandler` import style as existing SvelteKit text endpoints in this project.
